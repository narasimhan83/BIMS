@model BIMS.Models.City
@{
    ViewData["Title"] = "Delete City";
    var currentLang = Context.Session.GetString("Language") ?? "en";
}

<div class="delete-container">
    <div class="delete-header">
        <h1><i class="icon">⚠️</i> @(currentLang == "ar" ? "حذف المدينة" : "Delete City")</h1>
        <a asp-action="Cities" class="btn-back">
            <span>←</span> @(currentLang == "ar" ? "العودة إلى المدن" : "Back to Cities")
        </a>
    </div>

    @if (TempData["ErrorMessage"] != null)
    {
        <div class="alert alert-danger">
            ✗ @TempData["ErrorMessage"]
        </div>
    }

    <div class="warning-box">
        <h3>@(currentLang == "ar" ? "تحذير: سيتم حذف هذه المدينة نهائياً" : "⚠️ WARNING: This city will be permanently deleted")</h3>
        <p>@(currentLang == "ar" ? "هل أنت متأكد من رغبتك في حذف هذه المدينة؟ لا يمكن التراجع عن هذا الإجراء." : "Are you sure you want to delete this city? This action cannot be undone.")</p>
    </div>

    <div class="city-details">
        <div class="detail-grid">
            <div class="detail-item">
                <strong>@(currentLang == "ar" ? "اسم المدينة:" : "City Name:")</strong>
                <span>@(currentLang == "ar" && !string.IsNullOrEmpty(Model.NameAr) ? Model.NameAr : Model.Name)</span>
            </div>
            
            <div class="detail-item">
                <strong>@(currentLang == "ar" ? "الولاية:" : "State:")</strong>
                <span>@(currentLang == "ar" && !string.IsNullOrEmpty(Model.State?.NameAr) ? Model.State?.NameAr : Model.State?.Name)</span>
            </div>
            
            <div class="detail-item">
                <strong>@(currentLang == "ar" ? "الدولة:" : "Country:")</strong>
                <span>@(currentLang == "ar" && !string.IsNullOrEmpty(Model.State?.Country?.NameAr) ? Model.State?.Country?.NameAr : Model.State?.Country?.Name)</span>
            </div>
            
            @if (!string.IsNullOrEmpty(Model.Code))
            {
                <div class="detail-item">
                    <strong>@(currentLang == "ar" ? "رمز المدينة:" : "City Code:")</strong>
                    <span>@Model.Code</span>
                </div>
            }
            
            <div class="detail-item">
                <strong>@(currentLang == "ar" ? "الحالة:" : "Status:")</strong>
                <span>
                    @if (Model.IsActive)
                    {
                        <span class="badge badge-success">@(currentLang == "ar" ? "نشط" : "Active")</span>
                    }
                    else
                    {
                        <span class="badge badge-danger">@(currentLang == "ar" ? "غير نشط" : "Inactive")</span>
                    }
                </span>
            </div>
            
            <div class="detail-item">
                <strong>@(currentLang == "ar" ? "تاريخ الإنشاء:" : "Created Date:")</strong>
                <span>@Model.CreatedDate.ToLocalTime().ToString("dd MMM yyyy HH:mm")</span>
            </div>
            
            @if (Model.ModifiedDate.HasValue)
            {
                <div class="detail-item">
                    <strong>@(currentLang == "ar" ? "تاريخ التعديل:" : "Modified Date:")</strong>
                    <span>@Model.ModifiedDate.Value.ToLocalTime().ToString("dd MMM yyyy HH:mm")</span>
                </div>
            }
            
            @if (!string.IsNullOrEmpty(Model.Description))
            {
                <div class="detail-item full-width">
                    <strong>@(currentLang == "ar" ? "الوصف:" : "Description:")</strong>
                    <span>@(currentLang == "ar" && !string.IsNullOrEmpty(Model.DescriptionAr) ? Model.DescriptionAr : Model.Description)</span>
                </div>
            }
        </div>
    </div>

    <div class="delete-actions">
        <form asp-action="DeleteCity" method="post" style="display: inline;">
            <input type="hidden" asp-for="Id" />
            <button type="submit" class="btn-delete">
                <span>🗑️</span> @(currentLang == "ar" ? "حذف المدينة نهائياً" : "Delete City Permanently")
            </button>
        </form>
        <a asp-action="Cities" class="btn-cancel">
            <span>✖</span> @(currentLang == "ar" ? "إلغاء" : "Cancel")
        </a>
    </div>
</div>

<style>
    .delete-container {
        padding: 20px;
        max-width: 800px;
        margin: 0 auto;
    }

    .delete-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 30px;
    }

    .delete-header h1 {
        font-size: 28px;
        color: #dc3545;
        display: flex;
        align-items: center;
        gap: 10px;
    }

    .btn-back {
        padding: 10px 20px;
        background: #6c757d;
        color: white;
        border: none;
        border-radius: 6px;
        text-decoration: none;
        font-weight: 600;
        display: flex;
        align-items: center;
        gap: 8px;
        transition: all 0.3s ease;
    }

    .btn-back:hover {
        background: #545b62;
        transform: translateY(-1px);
    }

    .warning-box {
        background: #fff3cd;
        border: 2px solid #ffeaa7;
        border-radius: 12px;
        padding: 20px;
        margin-bottom: 30px;
        text-align: center;
    }

    .warning-box h3 {
        color: #856404;
        margin-bottom: 10px;
    }

    .warning-box p {
        color: #856404;
        margin: 0;
        font-size: 16px;
    }

    .city-details {
        background: white;
        border-radius: 12px;
        box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
        padding: 30px;
        margin-bottom: 30px;
    }

    .detail-grid {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 20px;
    }

    .detail-item.full-width {
        grid-column: 1 / -1;
    }

    .detail-item {
        display: flex;
        flex-direction: column;
        gap: 5px;
    }

    .detail-item strong {
        color: var(--navy-dark);
        font-size: 14px;
        text-transform: uppercase;
        letter-spacing: 0.5px;
    }

    .detail-item span {
        color: #495057;
        font-size: 16px;
        padding: 8px 0;
    }

    .delete-actions {
        display: flex;
        gap: 15px;
        justify-content: center;
        border-top: 1px solid #e9ecef;
        padding-top: 20px;
    }

    .btn-delete, .btn-cancel {
        padding: 12px 24px;
        border: none;
        border-radius: 8px;
        font-weight: 600;
        text-decoration: none;
        display: flex;
        align-items: center;
        gap: 8px;
        transition: all 0.3s ease;
        cursor: pointer;
    }

    .btn-delete {
        background: #dc3545;
        color: white;
    }

    .btn-delete:hover {
        background: #c82333;
        transform: translateY(-2px);
        box-shadow: 0 4px 12px rgba(220, 53, 69, 0.3);
    }

    .btn-cancel {
        background: #6c757d;
        color: white;
    }

    .btn-cancel:hover {
        background: #545b62;
        transform: translateY(-1px);
    }

    .badge {
        padding: 5px 12px;
        border-radius: 12px;
        font-size: 12px;
        font-weight: 600;
    }

    .badge-success {
        background: #28a745;
        color: white;
    }

    .badge-danger {
        background: #dc3545;
        color: white;
    }

    .alert {
        padding: 15px 20px;
        border-radius: 8px;
        margin-bottom: 20px;
        animation: slideIn 0.3s ease;
    }

    .alert-danger {
        background: #f8d7da;
        color: #721c24;
        border: 1px solid #f5c6cb;
    }

    @@media (max-width: 768px) {
        .detail-grid {
            grid-template-columns: 1fr;
        }
        
        .delete-header {
            flex-direction: column;
            gap: 15px;
            align-items: flex-start;
        }
        
        .delete-actions {
            flex-direction: column;
        }
    }
</style>