@model BIMS.Models.CalculationMethod
@{
    ViewData["Title"] = "Delete Calculation Method";
    var currentLang = Context.Session.GetString("Language") ?? "en";
}

<div class="delete-container">
    <div class="delete-header">
        <h1><i class="icon">⚠️</i> @(currentLang == "ar" ? "حذف طريقة الحساب" : "Delete Calculation Method")</h1>
        <a asp-action="CalculationMethods" class="btn-back">
            <span>←</span> @(currentLang == "ar" ? "العودة إلى طرق الحساب" : "Back to Calculation Methods")
        </a>
    </div>

    @if (TempData["ErrorMessage"] != null)
    {
        <div class="alert alert-danger">
            ✗ @TempData["ErrorMessage"]
        </div>
    }

    <div class="warning-box">
        <h3>@(currentLang == "ar" ? "تحذير: سيتم حذف هذه طريقة الحساب نهائياً" : "⚠️ WARNING: This calculation method will be permanently deleted")</h3>
        <p>@(currentLang == "ar" ? "هل أنت متأكد من أنك تريد حذف طريقة الحساب هذه؟ لا يمكن التراجع عن هذا الإجراء." : "Are you sure you want to delete this calculation method? This action cannot be undone.")</p>
    </div>

    <div class="details-container">
        <div class="detail-card">
            <h4>@(currentLang == "ar" ? "تفاصيل طريقة الحساب" : "Calculation Method Details")</h4>
            <div class="detail-grid">
                <div class="detail-item">
                    <strong>@(currentLang == "ar" ? "اسم الطريقة:" : "Method Name:")</strong>
                    <span>@(currentLang == "ar" && !string.IsNullOrEmpty(Model.NameAr) ? Model.NameAr : Model.Name)</span>
                </div>
                <div class="detail-item">
                    <strong>@(currentLang == "ar" ? "الرمز:" : "Code:")</strong>
                    <span>@(Model.Code ?? "-")</span>
                </div>
                <div class="detail-item">
                    <strong>@(currentLang == "ar" ? "صيغة الحساب:" : "Formula:")</strong>
                    <span>@(currentLang == "ar" && !string.IsNullOrEmpty(Model.FormulaAr) ? Model.FormulaAr : (Model.Formula ?? "-"))</span>
                </div>
                <div class="detail-item">
                    <strong>@(currentLang == "ar" ? "الوصف:" : "Description:")</strong>
                    <span>@(currentLang == "ar" && !string.IsNullOrEmpty(Model.DescriptionAr) ? Model.DescriptionAr : (Model.Description ?? "-"))</span>
                </div>
                <div class="detail-item">
                    <strong>@(currentLang == "ar" ? "الحالة:" : "Status:")</strong>
                    @if (Model.IsActive)
                    {
                        <span class="badge badge-success">@(currentLang == "ar" ? "نشط" : "Active")</span>
                    }
                    else
                    {
                        <span class="badge badge-danger">@(currentLang == "ar" ? "غير نشط" : "Inactive")</span>
                    }
                </div>
                <div class="detail-item">
                    <strong>@(currentLang == "ar" ? "تاريخ الإنشاء:" : "Created Date:")</strong>
                    <span>@Model.CreatedDate.ToLocalTime().ToString("dd MMM yyyy HH:mm")</span>
                </div>
                @if (Model.ModifiedDate.HasValue)
                {
                    <div class="detail-item">
                        <strong>@(currentLang == "ar" ? "تاريخ التعديل:" : "Modified Date:")</strong>
                        <span>@Model.ModifiedDate.Value.ToLocalTime().ToString("dd MMM yyyy HH:mm")</span>
                    </div>
                }
            </div>
        </div>
    </div>

    <div class="delete-actions">
        <form asp-action="DeleteCalculationMethod" method="post" class="delete-form" onsubmit="return confirm('@(currentLang == "ar" ? "هل أنت متأكد من حذف طريقة الحساب هذه؟" : "Are you sure you want to delete this calculation method?")')">
            <input type="hidden" asp-for="Id" />
            <button type="submit" class="btn-delete-confirm">
                <span>🗑️</span> @(currentLang == "ar" ? "نعم، احذف" : "Yes, Delete")
            </button>
            <a asp-action="CalculationMethods" class="btn-cancel">
                <span>✖</span> @(currentLang == "ar" ? "إلغاء" : "Cancel")
            </a>
        </form>
    </div>
</div>

<style>
    .delete-container {
        padding: 20px;
        max-width: 800px;
        margin: 0 auto;
    }

    .delete-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 30px;
    }

    .delete-header h1 {
        font-size: 28px;
        color: var(--navy-dark);
        display: flex;
        align-items: center;
        gap: 10px;
    }

    .btn-back {
        padding: 10px 20px;
        background: #6c757d;
        color: white;
        border: none;
        border-radius: 6px;
        text-decoration: none;
        font-weight: 600;
        display: flex;
        align-items: center;
        gap: 8px;
        transition: all 0.3s ease;
    }

    .btn-back:hover {
        background: #545b62;
        transform: translateY(-1px);
    }

    .warning-box {
        background: linear-gradient(135deg, #dc3545, #c82333);
        color: white;
        padding: 20px;
        border-radius: 12px;
        margin-bottom: 30px;
        text-align: center;
    }

    .warning-box h3 {
        margin: 0 0 10px 0;
        font-size: 18px;
    }

    .warning-box p {
        margin: 0;
        opacity: 0.9;
    }

    .details-container {
        margin-bottom: 30px;
    }

    .detail-card {
        background: white;
        border-radius: 12px;
        box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
        padding: 30px;
    }

    .detail-card h4 {
        margin: 0 0 20px 0;
        color: var(--navy-dark);
        font-size: 20px;
        border-bottom: 2px solid #f0f0f0;
        padding-bottom: 10px;
    }

    .detail-grid {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 15px;
    }

    .detail-item {
        display: flex;
        flex-direction: column;
        gap: 5px;
    }

    .detail-item strong {
        color: var(--navy-dark);
        font-weight: 600;
    }

    .detail-item span {
        color: #6c757d;
        padding: 8px;
        background: #f8f9fa;
        border-radius: 6px;
        font-family: monospace;
    }

    .delete-actions {
        display: flex;
        gap: 15px;
        justify-content: center;
        background: white;
        border-radius: 12px;
        box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
        padding: 20px;
    }

    .delete-form {
        display: flex;
        gap: 15px;
        margin: 0;
    }

    .btn-delete-confirm, .btn-cancel {
        padding: 12px 24px;
        border: none;
        border-radius: 8px;
        font-weight: 600;
        text-decoration: none;
        display: flex;
        align-items: center;
        gap: 8px;
        transition: all 0.3s ease;
        cursor: pointer;
    }

    .btn-delete-confirm {
        background: linear-gradient(135deg, #dc3545, #c82333);
        color: white;
    }

    .btn-delete-confirm:hover {
        transform: translateY(-2px);
        box-shadow: 0 4px 12px rgba(220, 53, 69, 0.3);
    }

    .btn-cancel {
        background: #6c757d;
        color: white;
    }

    .btn-cancel:hover {
        background: #545b62;
        transform: translateY(-1px);
    }

    .badge {
        padding: 5px 12px;
        border-radius: 12px;
        font-size: 12px;
        font-weight: 600;
    }

    .badge-success {
        background: #28a745;
        color: white;
    }

    .badge-danger {
        background: #dc3545;
        color: white;
    }

    @@media (max-width: 768px) {
        .detail-grid {
            grid-template-columns: 1fr;
        }
        
        .delete-header {
            flex-direction: column;
            gap: 15px;
            align-items: flex-start;
        }
        
        .delete-actions {
            flex-direction: column;
        }
    }
</style>