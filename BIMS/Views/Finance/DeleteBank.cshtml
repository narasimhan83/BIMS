@model BIMS.Models.Bank
@{ ViewData["Title"] = "Delete Bank"; var currentLang = Context.Session.GetString("Language") ?? "en"; }

<div class="delete-container">
    <div class="delete-card">
        <div class="delete-icon">
            <span>⚠️</span>
        </div>
        <h1 class="delete-title">@(currentLang == "ar" ? "تأكيد الحذف" : "Confirm Delete")</h1>
        <p class="delete-message">
            @(currentLang == "ar" ? "هل أنت متأكد من حذف هذا البنك؟" : "Are you sure you want to delete this bank?")
        </p>
        
        <div class="details-card">
            <div class="detail-row">
                <span class="detail-label">@(currentLang == "ar" ? "اسم البنك:" : "Bank Name:")</span>
                <span class="detail-value">@(currentLang == "ar" && !string.IsNullOrEmpty(Model.NameAr) ? Model.NameAr : Model.Name)</span>
            </div>
            
            @if (!string.IsNullOrEmpty(Model.Code))
            {
                <div class="detail-row">
                    <span class="detail-label">@(currentLang == "ar" ? "الرمز:" : "Code:")</span>
                    <span class="detail-value">@Model.Code</span>
                </div>
            }
            
            @if (!string.IsNullOrEmpty(Model.IBAN))
            {
                <div class="detail-row">
                    <span class="detail-label">IBAN:</span>
                    <span class="detail-value"><code>@Model.IBAN</code></span>
                </div>
            }
            
            @if (!string.IsNullOrEmpty(Model.SwiftCode))
            {
                <div class="detail-row">
                    <span class="detail-label">@(currentLang == "ar" ? "رمز SWIFT:" : "SWIFT Code:")</span>
                    <span class="detail-value">@Model.SwiftCode</span>
                </div>
            }
            
            @if (!string.IsNullOrEmpty(Model.BranchName) || !string.IsNullOrEmpty(Model.BranchNameAr))
            {
                <div class="detail-row">
                    <span class="detail-label">@(currentLang == "ar" ? "الفرع:" : "Branch:")</span>
                    <span class="detail-value">@(currentLang == "ar" && !string.IsNullOrEmpty(Model.BranchNameAr) ? Model.BranchNameAr : Model.BranchName)</span>
                </div>
            }
            
            <div class="detail-row">
                <span class="detail-label">@(currentLang == "ar" ? "الحالة:" : "Status:")</span>
                @if (Model.IsActive)
                {
                    <span class="badge badge-success">@(currentLang == "ar" ? "نشط" : "Active")</span>
                }
                else
                {
                    <span class="badge badge-danger">@(currentLang == "ar" ? "غير نشط" : "Inactive")</span>
                }
            </div>
        </div>

        <form asp-action="DeleteBank" method="post">
            <input type="hidden" asp-for="Id" />
            <div class="delete-actions">
                <button type="submit" class="btn-delete-confirm">
                    <span>🗑️</span> @(currentLang == "ar" ? "نعم، احذف" : "Yes, Delete")
                </button>
                <a asp-action="Banks" class="btn-cancel">
                    @(currentLang == "ar" ? "إلغاء" : "Cancel")
                </a>
            </div>
        </form>
    </div>
</div>

<style>
    .delete-container { display: flex; justify-content: center; align-items: center; min-height: 70vh; padding: 20px; }
    .delete-card { background: white; border-radius: 16px; box-shadow: 0 8px 30px rgba(0, 0, 0, 0.12); padding: 50px 40px; max-width: 600px; width: 100%; text-align: center; }
    .delete-icon { font-size: 80px; margin-bottom: 20px; animation: shake 0.5s ease-in-out; }
    .delete-title { font-size: 28px; font-weight: 700; color: #dc3545; margin-bottom: 15px; }
    .delete-message { font-size: 16px; color: #666; margin-bottom: 30px; }
    .details-card { background: #f8f9fa; border-radius: 12px; padding: 25px; margin-bottom: 30px; text-align: left; }
    .detail-row { display: flex; justify-content: space-between; align-items: center; padding: 12px 0; border-bottom: 1px solid #e0e0e0; }
    .detail-row:last-child { border-bottom: none; }
    .detail-label { font-weight: 600; color: var(--navy-dark); font-size: 14px; }
    .detail-value { color: #333; font-size: 14px; }
    .badge { padding: 6px 12px; border-radius: 20px; font-size: 12px; font-weight: 600; }
    .badge-success { background: #d4edda; color: #155724; border: 1px solid #c3e6cb; }
    .badge-danger { background: #f8d7da; color: #721c24; border: 1px solid #f5c6cb; }
    .delete-actions { display: flex; gap: 15px; justify-content: center; }
    .btn-delete-confirm { flex: 1; padding: 14px 24px; background: linear-gradient(135deg, #dc3545, #c82333); color: white; border: none; border-radius: 10px; font-size: 16px; font-weight: 600; cursor: pointer; display: flex; align-items: center; justify-content: center; gap: 8px; transition: all 0.3s ease; }
    .btn-delete-confirm:hover { transform: translateY(-2px); box-shadow: 0 6px 20px rgba(220, 53, 69, 0.4); }
    .btn-cancel { flex: 1; padding: 14px 24px; background: #6c757d; color: white; border-radius: 10px; text-decoration: none; font-weight: 600; display: flex; align-items: center; justify-content: center; transition: all 0.3s ease; }
    .btn-cancel:hover { background: #5a6268; transform: translateY(-2px); }
    code { background: #e9ecef; padding: 4px 8px; border-radius: 4px; font-size: 13px; }
    @@keyframes shake { 0%, 100% { transform: rotate(0deg); } 25% { transform: rotate(-5deg); } 75% { transform: rotate(5deg); } }
    @@media (max-width: 768px) { .delete-card { padding: 30px 20px; } .delete-actions { flex-direction: column; } }
</style>