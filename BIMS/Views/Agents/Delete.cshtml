@model BIMS.Models.Agent
@{
    ViewData["Title"] = "Delete Agent";
    var currentLang = Context.Session.GetString("Language") ?? "en";
}

<div class="delete-container">
    <div class="delete-header">
        <h1><i class="icon">⚠️</i> @(currentLang == "ar" ? "حذف الوكيل" : "Delete Agent")</h1>
        <a asp-action="Index" class="btn-back">
            <span>←</span> @(currentLang == "ar" ? "العودة إلى الوكلاء" : "Back to Agents")
        </a>
    </div>

    @if (TempData["ErrorMessage"] != null)
    {
        <div class="alert alert-danger">
            ✗ @TempData["ErrorMessage"]
        </div>
    }

    <div class="warning-box">
        <h3>@(currentLang == "ar" ? "تحذير: سيتم حذف هذا الوكيل نهائياً" : "⚠️ WARNING: This agent will be permanently deleted")</h3>
        <p>@(currentLang == "ar" ? "هل أنت متأكد من رغبتك في حذف هذا الوكيل؟ لا يمكن التراجع عن هذا الإجراء." : "Are you sure you want to delete this agent? This action cannot be undone.")</p>
    </div>

    <div class="agent-details">
        <div class="detail-grid">
            <div class="detail-item">
                <strong>@(currentLang == "ar" ? "اسم الوكيل:" : "Agent Name:")</strong>
                <span>@Model.Name</span>
            </div>

            <div class="detail-item">
                <strong>@(currentLang == "ar" ? "الهاتف:" : "Phone:")</strong>
                <span>@Model.Phone</span>
            </div>

            @if (!string.IsNullOrEmpty(Model.Mobile))
            {
                <div class="detail-item">
                    <strong>@(currentLang == "ar" ? "الجوال:" : "Mobile:")</strong>
                    <span>@Model.Mobile</span>
                </div>
            }

            <div class="detail-item">
                <strong>@(currentLang == "ar" ? "البريد الإلكتروني:" : "Email:")</strong>
                <span>@Model.Email</span>
            </div>

            <div class="detail-item">
                <strong>@(currentLang == "ar" ? "المدينة:" : "City:")</strong>
                <span>@Model.City?.Name</span>
            </div>

            <div class="detail-item">
                <strong>@(currentLang == "ar" ? "الحالة:" : "Status:")</strong>
                @if (Model.IsActive)
                {
                    <span class="badge badge-success">@(currentLang == "ar" ? "نشط" : "Active")</span>
                }
                else
                {
                    <span class="badge badge-danger">@(currentLang == "ar" ? "غير نشط" : "Inactive")</span>
                }
            </div>

            <div class="detail-item full-width">
                <strong>@(currentLang == "ar" ? "العنوان:" : "Address:")</strong>
                <span>@Model.Address</span>
            </div>

            @if (Model.BankDetails?.Any() == true)
            {
                <div class="detail-item full-width">
                    <strong>@(currentLang == "ar" ? "البنوك المرتبطة:" : "Associated Banks:")</strong>
                    <div class="associated-banks">
                        @foreach (var bankDetail in Model.BankDetails)
                        {
                            <div class="bank-info">
                                <span class="bank-name">@bankDetail.Bank?.Name</span>
                                @if (bankDetail.IsPrimary)
                                {
                                    <span class="badge badge-primary">@(currentLang == "ar" ? "رئيسي" : "Primary")</span>
                                }
                            </div>
                        }
                    </div>
                </div>
            }

            <div class="detail-item">
                <strong>@(currentLang == "ar" ? "تاريخ الإنشاء:" : "Created Date:")</strong>
                <span>@Model.CreatedDate.ToLocalTime().ToString("dd MMM yyyy HH:mm")</span>
            </div>

            @if (Model.ModifiedDate.HasValue)
            {
                <div class="detail-item">
                    <strong>@(currentLang == "ar" ? "تاريخ التعديل:" : "Modified Date:")</strong>
                    <span>@Model.ModifiedDate.Value.ToLocalTime().ToString("dd MMM yyyy HH:mm")</span>
                </div>
            }
        </div>
    </div>

    <div class="delete-actions">
        <form asp-action="Delete" method="post" style="display: inline;">
            <input type="hidden" asp-for="Id" />
            <button type="submit" class="btn-delete" onclick="return confirm('@(currentLang == "ar" ? "هل أنت متأكد من حذف هذا الوكيل؟" : "Are you sure you want to delete this agent?")')">
                <span>🗑️</span> @(currentLang == "ar" ? "نعم، احذف الوكيل" : "Yes, Delete Agent")
            </button>
        </form>
        <a asp-action="Index" class="btn-cancel">
            <span>✖</span> @(currentLang == "ar" ? "إلغاء" : "Cancel")
        </a>
    </div>
</div>

<style>
    .delete-container {
        padding: 20px;
        max-width: 800px;
        margin: 0 auto;
    }

    .delete-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 30px;
    }

    .delete-header h1 {
        font-size: 28px;
        color: #dc3545;
        display: flex;
        align-items: center;
        gap: 10px;
    }

    .btn-back {
        padding: 10px 20px;
        background: #6c757d;
        color: white;
        border: none;
        border-radius: 6px;
        text-decoration: none;
        font-weight: 600;
        display: flex;
        align-items: center;
        gap: 8px;
        transition: all 0.3s ease;
    }

    .btn-back:hover {
        background: #545b62;
        transform: translateY(-1px);
    }

    .warning-box {
        background: linear-gradient(135deg, #dc3545, #c82333);
        color: white;
        padding: 20px;
        border-radius: 12px;
        margin-bottom: 30px;
        text-align: center;
    }

    .warning-box h3 {
        margin: 0 0 10px 0;
        font-size: 18px;
    }

    .warning-box p {
        margin: 0;
        opacity: 0.9;
    }

    .agent-details {
        background: white;
        border-radius: 12px;
        box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
        padding: 30px;
        margin-bottom: 30px;
    }

    .detail-grid {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 15px;
    }

    .detail-item.full-width {
        grid-column: 1 / -1;
    }

    .detail-item {
        display: flex;
        flex-direction: column;
        gap: 5px;
    }

    .detail-item strong {
        color: var(--navy-dark);
        font-weight: 600;
        font-size: 14px;
    }

    .detail-item span {
        color: #495057;
        padding: 8px;
        background: #f8f9fa;
        border-radius: 6px;
        border: 1px solid #e9ecef;
    }

    .associated-banks {
        display: flex;
        flex-direction: column;
        gap: 10px;
        margin-top: 10px;
    }

    .bank-info {
        display: flex;
        align-items: center;
        gap: 10px;
        padding: 10px;
        background: #f8f9fa;
        border-radius: 6px;
        border: 1px solid #e9ecef;
    }

    .bank-name {
        font-weight: 600;
        color: var(--navy-dark);
    }

    .delete-actions {
        display: flex;
        gap: 15px;
        justify-content: center;
        background: white;
        border-radius: 12px;
        box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
        padding: 20px;
    }

    .btn-delete, .btn-cancel {
        padding: 12px 24px;
        border: none;
        border-radius: 8px;
        font-weight: 600;
        text-decoration: none;
        display: flex;
        align-items: center;
        gap: 8px;
        transition: all 0.3s ease;
        cursor: pointer;
    }

    .btn-delete {
        background: #dc3545;
        color: white;
    }

    .btn-delete:hover {
        background: #c82333;
        transform: translateY(-2px);
        box-shadow: 0 4px 12px rgba(220, 53, 69, 0.3);
    }

    .btn-cancel {
        background: #6c757d;
        color: white;
    }

    .btn-cancel:hover {
        background: #545b62;
        transform: translateY(-1px);
    }

    .badge {
        padding: 5px 12px;
        border-radius: 12px;
        font-size: 12px;
        font-weight: 600;
    }

    .badge-success {
        background: #28a745;
        color: white;
    }

    .badge-danger {
        background: #dc3545;
        color: white;
    }

    .badge-primary {
        background: #007bff;
        color: white;
    }

    .alert {
        padding: 15px 20px;
        border-radius: 8px;
        margin-bottom: 20px;
        animation: slideIn 0.3s ease;
    }

    .alert-danger {
        background: #f8d7da;
        color: #721c24;
        border: 1px solid #f5c6cb;
    }

    @@media (max-width: 768px) {
        .detail-grid {
            grid-template-columns: 1fr;
        }

        .delete-header {
            flex-direction: column;
            gap: 15px;
            align-items: flex-start;
        }

        .delete-actions {
            flex-direction: column;
        }

        .bank-info {
            flex-direction: column;
            align-items: flex-start;
            gap: 5px;
        }
    }
</style>