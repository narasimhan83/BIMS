@model BIMS.Models.Lead
@{
    ViewData["Title"] = "Delete Lead";
    var currentLang = Context.Session.GetString("Language") ?? "en";
}

<div class="delete-container">
    <div class="delete-header">
        <h1><i class="icon">🗑️</i> @(currentLang == "ar" ? "حذف العميل المحتمل" : "Delete Lead")</h1>
        <a asp-action="Index" class="btn-back">
            <span>←</span> @(currentLang == "ar" ? "العودة إلى العملاء المحتملين" : "Back to Leads")
        </a>
    </div>

    <div class="delete-content">
        <div class="delete-warning">
            <div class="warning-icon">⚠️</div>
            <h3>@(currentLang == "ar" ? "هل أنت متأكد من حذف هذا العميل المحتمل؟" : "Are you sure you want to delete this lead?")</h3>
            <p>@(currentLang == "ar" ? "هذا الإجراء لا يمكن التراجع عنه." : "This action cannot be undone.")</p>
        </div>

        <div class="delete-details">
            <dl class="row">
                <dt class="col-sm-3">@(currentLang == "ar" ? "الاسم" : "Name"):</dt>
                <dd class="col-sm-9">@(currentLang == "ar" && !string.IsNullOrEmpty(Model.NameAr) ? Model.NameAr : Model.NameEn)</dd>

                <dt class="col-sm-3">@(currentLang == "ar" ? "البريد الإلكتروني" : "Email"):</dt>
                <dd class="col-sm-9">@Model.Email</dd>

                <dt class="col-sm-3">@(currentLang == "ar" ? "الهاتف" : "Phone"):</dt>
                <dd class="col-sm-9">@Model.Phone</dd>

                <dt class="col-sm-3">@(currentLang == "ar" ? "الشركة" : "Company"):</dt>
                <dd class="col-sm-9">@(currentLang == "ar" && !string.IsNullOrEmpty(Model.CompanyAr) ? Model.CompanyAr : (Model.CompanyEn ?? "-"))</dd>

                <dt class="col-sm-3">@(currentLang == "ar" ? "المصدر" : "Source"):</dt>
                <dd class="col-sm-9">@Model.Source</dd>

                <dt class="col-sm-3">@(currentLang == "ar" ? "الحالة" : "Status"):</dt>
                <dd class="col-sm-9">@Model.Status</dd>

                <dt class="col-sm-3">@(currentLang == "ar" ? "القيمة المحتملة" : "Potential Value"):</dt>
                <dd class="col-sm-9">@Model.PotentialValue.ToString("N2") SAR</dd>
            </dl>
        </div>

        <form asp-action="Delete" method="post" class="delete-form">
            <input type="hidden" asp-for="Id" />
            <div class="delete-actions">
                <button type="submit" class="btn-delete-confirm">
                    <span>🗑️</span> @(currentLang == "ar" ? "حذف" : "Delete")
                </button>
                <a asp-action="Index" class="btn-cancel">
                    <span>✖</span> @(currentLang == "ar" ? "إلغاء" : "Cancel")
                </a>
            </div>
        </form>
    </div>
</div>

<style>
    .delete-container {
        padding: 20px;
        max-width: 800px;
        margin: 0 auto;
    }

    .delete-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 30px;
    }

    .delete-header h1 {
        font-size: 28px;
        color: var(--navy-dark);
        display: flex;
        align-items: center;
        gap: 10px;
    }

    .btn-back {
        padding: 10px 20px;
        background: #6c757d;
        color: white;
        border: none;
        border-radius: 6px;
        text-decoration: none;
        font-weight: 600;
        display: flex;
        align-items: center;
        gap: 8px;
        transition: all 0.3s ease;
    }

    .btn-back:hover {
        background: #545b62;
        transform: translateY(-1px);
    }

    .delete-content {
        background: white;
        border-radius: 12px;
        box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
        padding: 30px;
    }

    .delete-warning {
        text-align: center;
        margin-bottom: 30px;
        padding: 20px;
        background: #fff3cd;
        border: 1px solid #ffeaa7;
        border-radius: 8px;
    }

    .warning-icon {
        font-size: 48px;
        margin-bottom: 15px;
    }

    .delete-warning h3 {
        color: #856404;
        margin-bottom: 10px;
    }

    .delete-warning p {
        color: #856404;
        margin: 0;
    }

    .delete-details {
        margin-bottom: 30px;
    }

    .delete-details dl {
        margin: 0;
    }

    .delete-details dt {
        font-weight: 600;
        color: var(--navy-dark);
        padding: 8px 0;
        border-bottom: 1px solid #f0f0f0;
    }

    .delete-details dd {
        padding: 8px 0;
        border-bottom: 1px solid #f0f0f0;
        color: #495057;
    }

    .delete-actions {
        display: flex;
        gap: 15px;
        justify-content: center;
        border-top: 1px solid #e9ecef;
        padding-top: 20px;
    }

    .btn-delete-confirm, .btn-cancel {
        padding: 12px 24px;
        border: none;
        border-radius: 8px;
        font-weight: 600;
        text-decoration: none;
        display: flex;
        align-items: center;
        gap: 8px;
        transition: all 0.3s ease;
        cursor: pointer;
    }

    .btn-delete-confirm {
        background: #dc3545;
        color: white;
    }

    .btn-delete-confirm:hover {
        background: #c82333;
        transform: translateY(-2px);
        box-shadow: 0 4px 12px rgba(220, 53, 69, 0.3);
    }

    .btn-cancel {
        background: #6c757d;
        color: white;
    }

    .btn-cancel:hover {
        background: #545b62;
        transform: translateY(-1px);
    }

    @@media (max-width: 768px) {
        .delete-header {
            flex-direction: column;
            gap: 15px;
            align-items: flex-start;
        }

        .delete-actions {
            flex-direction: column;
        }
    }
</style>