@model IEnumerable<BIMS.Models.ProductType>
@{ ViewData["Title"] = "Product Types"; var currentLang = Context.Session.GetString("Language") ?? "en"; }

<div class="page-container">
    <div class="page-header">
        <h1><i class="icon">🏷️</i> @(currentLang == "ar" ? "أنواع المنتجات" : "Product Types")</h1>
        <a asp-action="CreateProductType" class="btn-create">
            <span>➕</span> @(currentLang == "ar" ? "إضافة نوع منتج" : "Create Product Type")
        </a>
    </div>

    @if (TempData["SuccessMessage"] != null)
    {
        <div class="alert alert-success">@TempData["SuccessMessage"]</div>
    }
    @if (TempData["ErrorMessage"] != null)
    {
        <div class="alert alert-error">@TempData["ErrorMessage"]</div>
    }

    <div class="table-container">
        <table class="data-table">
            <thead>
                <tr>
                    <th>@(currentLang == "ar" ? "الصورة" : "Image")</th>
                    <th>@(currentLang == "ar" ? "الكود" : "Code")</th>
                    <th>@(currentLang == "ar" ? "اسم النوع" : "Type Name")</th>
                    <th>@(currentLang == "ar" ? "فئة المنتج" : "Product Class")</th>
                    <th>@(currentLang == "ar" ? "نوع العمل" : "Business Type")</th>
                    <th>@(currentLang == "ar" ? "الوصف" : "Description")</th>
                    <th>@(currentLang == "ar" ? "الحالة" : "Status")</th>
                    <th>@(currentLang == "ar" ? "الإجراءات" : "Actions")</th>
                </tr>
            </thead>
            <tbody>
                @if (Model != null && Model.Any())
                {
                    foreach (var item in Model)
                    {
                        <tr>
                            <td>
                                @if (!string.IsNullOrEmpty(item.ImagePath))
                                {
                                    <img src="@item.ImagePath" alt="@item.ProductTypeName" class="table-image" />
                                }
                                else
                                {
                                    <span class="no-image">🏷️</span>
                                }
                            </td>
                            <td>@(item.Code ?? "-")</td>
                            <td>
                                <strong>@(currentLang == "ar" && !string.IsNullOrEmpty(item.ProductTypeNameAr) ? item.ProductTypeNameAr : item.ProductTypeName)</strong>
                            </td>
                            <td>@(currentLang == "ar" && !string.IsNullOrEmpty(item.ProductClass?.ProductClassNameAr) ? item.ProductClass.ProductClassNameAr : item.ProductClass?.ProductClassName)</td>
                            <td>@(currentLang == "ar" && !string.IsNullOrEmpty(item.ProductClass?.BusinessType?.NameAr) ? item.ProductClass.BusinessType.NameAr : item.ProductClass?.BusinessType?.Name)</td>
                            <td class="description-cell">@(currentLang == "ar" && !string.IsNullOrEmpty(item.DescriptionAr) ? item.DescriptionAr : item.Description)</td>
                            <td>
                                @if (item.IsActive)
                                {
                                    <span class="status-badge status-active">@(currentLang == "ar" ? "نشط" : "Active")</span>
                                }
                                else
                                {
                                    <span class="status-badge status-inactive">@(currentLang == "ar" ? "غير نشط" : "Inactive")</span>
                                }
                            </td>
                            <td>
                                <div class="action-buttons">
                                    <a asp-action="EditProductType" asp-route-id="@item.Id" class="btn-action btn-edit" title="@(currentLang == "ar" ? "تعديل" : "Edit")">✏️</a>
                                    <a asp-action="DeleteProductType" asp-route-id="@item.Id" class="btn-action btn-delete" title="@(currentLang == "ar" ? "حذف" : "Delete")">🗑️</a>
                                </div>
                            </td>
                        </tr>
                    }
                }
                else
                {
                    <tr>
                        <td colspan="8" class="no-data">@(currentLang == "ar" ? "لا توجد أنواع منتجات" : "No product types found")</td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

@section Styles {
    <style>
        .page-container { padding: 20px; max-width: 1400px; margin: 0 auto; }
        .page-header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 30px; }
        .page-header h1 { font-size: 28px; color: var(--navy-dark); display: flex; align-items: center; gap: 10px; margin: 0; }
        .btn-create { padding: 12px 24px; background: linear-gradient(135deg, var(--primary-teal), var(--teal-dark)); color: white; border-radius: 8px; text-decoration: none; font-weight: 600; display: flex; align-items: center; gap: 8px; transition: all 0.3s ease; }
        .btn-create:hover { transform: translateY(-2px); box-shadow: 0 6px 15px rgba(0, 166, 166, 0.3); }
        .alert { padding: 15px 20px; border-radius: 8px; margin-bottom: 20px; font-weight: 500; }
        .alert-success { background: #d4edda; border: 1px solid #c3e6cb; color: #155724; }
        .alert-error { background: #f8d7da; border: 1px solid #f5c6cb; color: #721c24; }
        .table-container { background: white; border-radius: 12px; box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1); overflow: hidden; }
        .data-table { width: 100%; border-collapse: collapse; }
        .data-table thead { background: linear-gradient(135deg, var(--navy-dark), #1a4066); color: white; }
        .data-table th { padding: 16px; text-align: left; font-weight: 600; font-size: 14px; }
        .data-table tbody tr { border-bottom: 1px solid #e0e0e0; transition: background 0.2s; }
        .data-table tbody tr:hover { background: #f8f9fa; }
        .data-table td { padding: 14px 16px; }
        .table-image { width: 50px; height: 50px; object-fit: cover; border-radius: 6px; }
        .no-image { font-size: 32px; display: block; text-align: center; }
        .description-cell { max-width: 300px; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
        .badge { display: inline-block; padding: 4px 12px; border-radius: 12px; font-size: 12px; font-weight: 600; }
        .badge-info { background: #cfe2ff; color: #084298; }
        .status-badge { display: inline-block; padding: 6px 12px; border-radius: 6px; font-size: 12px; font-weight: 600; }
        .status-active { background: #d4edda; color: #155724; }
        .status-inactive { background: #f8d7da; color: #721c24; }
        .action-buttons { display: flex; gap: 8px; }
        .btn-action { padding: 8px 12px; border-radius: 6px; text-decoration: none; font-size: 16px; transition: all 0.3s ease; display: inline-block; }
        .btn-edit { background: #ffc107; color: white; }
        .btn-edit:hover { background: #ff9800; transform: scale(1.1); }
        .btn-delete { background: #dc3545; color: white; }
        .btn-delete:hover { background: #c82333; transform: scale(1.1); }
        .no-data { text-align: center; padding: 40px; color: #999; font-style: italic; }
    </style>
}