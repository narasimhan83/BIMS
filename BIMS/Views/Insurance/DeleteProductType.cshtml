@model BIMS.Models.ProductType
@{ ViewData["Title"] = "Delete Product Type"; var currentLang = Context.Session.GetString("Language") ?? "en"; }

<div class="form-container">
    <div class="form-header">
        <h1><i class="icon">🗑️</i> @(currentLang == "ar" ? "حذف نوع المنتج" : "Delete Product Type")</h1>
        <a asp-action="ProductTypes" class="btn-back">← @(currentLang == "ar" ? "العودة" : "Back")</a>
    </div>

    <div class="delete-card">
        <div class="warning-message">
            <span class="warning-icon">⚠️</span>
            <p>@(currentLang == "ar" ? "هل أنت متأكد من حذف نوع المنتج هذا؟" : "Are you sure you want to delete this Product Type?")</p>
        </div>

        <div class="details-grid">
            <div class="detail-row">
                <span class="detail-label">@(currentLang == "ar" ? "الصورة:" : "Image:")</span>
                <span class="detail-value">
                    @if (!string.IsNullOrEmpty(Model.ImagePath))
                    {
                        <img src="@Model.ImagePath" alt="@Model.ProductTypeName" style="width: 100px; height: 100px; object-fit: cover; border-radius: 8px;" />
                    }
                    else
                    {
                        <span>@(currentLang == "ar" ? "لا توجد صورة" : "No image")</span>
                    }
                </span>
            </div>
            
            <div class="detail-row">
                <span class="detail-label">@(currentLang == "ar" ? "الكود:" : "Code:")</span>
                <span class="detail-value">@(Model.Code ?? "-")</span>
            </div>

            <div class="detail-row">
                <span class="detail-label">@Html.DisplayNameFor(model => model.ProductTypeName):</span>
                <span class="detail-value">@Model.ProductTypeName</span>
            </div>

            @if (!string.IsNullOrEmpty(Model.ProductTypeNameAr))
            {
                <div class="detail-row">
                    <span class="detail-label">@Html.DisplayNameFor(model => model.ProductTypeNameAr):</span>
                    <span class="detail-value" dir="rtl">@Model.ProductTypeNameAr</span>
                </div>
            }

            <div class="detail-row">
                <span class="detail-label">@(currentLang == "ar" ? "فئة المنتج:" : "Product Class:")</span>
                <span class="detail-value">@(currentLang == "ar" && !string.IsNullOrEmpty(Model.ProductClass?.ProductClassNameAr) ? Model.ProductClass.ProductClassNameAr : Model.ProductClass?.ProductClassName)</span>
            </div>

            <div class="detail-row">
                <span class="detail-label">@(currentLang == "ar" ? "نوع العمل:" : "Business Type:")</span>
                <span class="detail-value">@(currentLang == "ar" && !string.IsNullOrEmpty(Model.ProductClass?.BusinessType?.NameAr) ? Model.ProductClass.BusinessType.NameAr : Model.ProductClass?.BusinessType?.Name)</span>
            </div>

            @if (!string.IsNullOrEmpty(Model.Description))
            {
                <div class="detail-row">
                    <span class="detail-label">@Html.DisplayNameFor(model => model.Description):</span>
                    <span class="detail-value">@Model.Description</span>
                </div>
            }

            @if (!string.IsNullOrEmpty(Model.DescriptionAr))
            {
                <div class="detail-row">
                    <span class="detail-label">@Html.DisplayNameFor(model => model.DescriptionAr):</span>
                    <span class="detail-value" dir="rtl">@Model.DescriptionAr</span>
                </div>
            }

            <div class="detail-row">
                <span class="detail-label">@Html.DisplayNameFor(model => model.IsActive):</span>
                <span class="detail-value">
                    @if (Model.IsActive)
                    {
                        <span class="status-badge status-active">@(currentLang == "ar" ? "نشط" : "Active")</span>
                    }
                    else
                    {
                        <span class="status-badge status-inactive">@(currentLang == "ar" ? "غير نشط" : "Inactive")</span>
                    }
                </span>
            </div>
        </div>

        <form asp-action="DeleteProductType" method="post" class="delete-form">
            <input type="hidden" asp-for="Id" />
            <div class="form-actions">
                <button type="submit" class="btn-delete-confirm">
                    <span>🗑️</span> @(currentLang == "ar" ? "تأكيد الحذف" : "Confirm Delete")
                </button>
                <a asp-action="ProductTypes" class="btn-cancel">@(currentLang == "ar" ? "إلغاء" : "Cancel")</a>
            </div>
        </form>
    </div>
</div>

<style>
    .form-container { padding: 20px; max-width: 800px; margin: 0 auto; }
    .form-header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 30px; }
    .form-header h1 { font-size: 28px; color: var(--navy-dark); display: flex; align-items: center; gap: 10px; }
    .btn-back { padding: 10px 20px; background: #6c757d; color: white; border-radius: 8px; text-decoration: none; transition: all 0.3s ease; }
    .btn-back:hover { background: #5a6268; }
    .delete-card { background: white; border-radius: 12px; box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1); padding: 40px; }
    .warning-message { background: #fff3cd; border: 2px solid #ffc107; border-radius: 8px; padding: 20px; margin-bottom: 30px; display: flex; align-items: center; gap: 15px; }
    .warning-icon { font-size: 36px; }
    .warning-message p { margin: 0; font-size: 16px; font-weight: 500; color: #856404; }
    .alert { padding: 15px 20px; border-radius: 8px; margin-bottom: 20px; }
    .alert-error { background: #f8d7da; border: 1px solid #f5c6cb; color: #721c24; }
    .details-grid { display: flex; flex-direction: column; gap: 20px; margin-bottom: 30px; }
    .detail-row { display: grid; grid-template-columns: 200px 1fr; gap: 20px; padding: 15px; background: #f8f9fa; border-radius: 8px; }
    .detail-label { font-weight: 600; color: var(--navy-dark); }
    .detail-value { color: #333; }
    .badge { display: inline-block; padding: 4px 12px; border-radius: 12px; font-size: 12px; font-weight: 600; }
    .badge-info { background: #cfe2ff; color: #084298; }
    .status-badge { display: inline-block; padding: 6px 12px; border-radius: 6px; font-size: 12px; font-weight: 600; }
    .status-active { background: #d4edda; color: #155724; }
    .status-inactive { background: #f8d7da; color: #721c24; }
    .form-actions { display: flex; gap: 15px; margin-top: 30px; }
    .btn-delete-confirm { flex: 1; padding: 14px 24px; background: linear-gradient(135deg, #dc3545, #c82333); color: white; border: none; border-radius: 8px; font-size: 16px; font-weight: 600; cursor: pointer; display: flex; align-items: center; justify-content: center; gap: 8px; transition: all 0.3s ease; }
    .btn-delete-confirm:hover { transform: translateY(-2px); box-shadow: 0 6px 15px rgba(220, 53, 69, 0.3); }
    .btn-cancel { padding: 14px 24px; background: #6c757d; color: white; border-radius: 8px; text-decoration: none; font-weight: 600; transition: all 0.3s ease; display: inline-block; text-align: center; }
    .btn-cancel:hover { background: #5a6268; }
    @@media (max-width: 768px) { .detail-row { grid-template-columns: 1fr; gap: 10px; } }
</style>