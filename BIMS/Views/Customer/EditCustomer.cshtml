@model BIMS.Models.Customer
@{ ViewData["Title"] = "Edit Customer"; var currentLang = Context.Session.GetString("Language") ?? "en"; }

<div class="form-container">
    <div class="form-header">
        <h1><i class="icon">✏️</i> @(currentLang == "ar" ? "تعديل العميل" : "Edit Customer")</h1>
        <a asp-action="Customers" class="btn-back">← @(currentLang == "ar" ? "العودة" : "Back")</a>
    </div>

    <div class="form-card">
        <form asp-action="EditCustomer" method="post" enctype="multipart/form-data">
            <div asp-validation-summary="ModelOnly" class="text-danger validation-summary"></div>
            <input type="hidden" asp-for="Id" />
            <input type="hidden" asp-for="CreatedDate" />
            <input type="hidden" asp-for="CreatedBy" />
            <input type="hidden" asp-for="ConvertedToCustomer" />
            
            <!-- Customer Classification -->
            <div class="section-header">@(currentLang == "ar" ? "تصنيف العميل" : "Customer Classification")</div>
            <div class="form-row">
                <div class="form-group">
                    <label asp-for="CustomerTypeId" class="form-label"></label>
                    <select asp-for="CustomerTypeId" class="form-control" asp-items="ViewBag.CustomerTypes" id="CustomerTypeId"></select>
                    <span asp-validation-for="CustomerTypeId" class="text-danger"></span>
                </div>
                <div class="form-group" id="groupSection">
                    <label asp-for="CustomerGroupId" class="form-label"></label>
                    <select asp-for="CustomerGroupId" class="form-control" asp-items="ViewBag.CustomerGroups" id="CustomerGroupId">
                        <option value="">-- @(currentLang == "ar" ? "بدون مجموعة" : "No Group") --</option>
                    </select>
                </div>
                <div class="form-group">
                    <label asp-for="AgentId" class="form-label"></label>
                    <select asp-for="AgentId" class="form-control" asp-items="ViewBag.Agents" id="AgentId">
                        <option value="">-- @(currentLang == "ar" ? "بدون وكيل" : "No Agent") --</option>
                    </select>
                </div>
            </div>

            <!-- Personal Information -->
            <div class="section-header">@(currentLang == "ar" ? "المعلومات الشخصية" : "Personal Information")</div>
            <div class="bilingual-row">
                <div class="bilingual-col english">
                    <label asp-for="CustomerName" class="form-label"></label>
                    <input asp-for="CustomerName" class="form-control" />
                    <span asp-validation-for="CustomerName" class="text-danger"></span>
                </div>
                <div class="bilingual-col arabic">
                    <label asp-for="CustomerNameAr" class="form-label"></label>
                    <input asp-for="CustomerNameAr" class="form-control" dir="rtl" />
                </div>
            </div>

            <div class="bilingual-row">
                <div class="bilingual-col english">
                    <label asp-for="InsuredName" class="form-label"></label>
                    <input asp-for="InsuredName" class="form-control" />
                </div>
                <div class="bilingual-col arabic">
                    <label asp-for="InsuredNameAr" class="form-label"></label>
                    <input asp-for="InsuredNameAr" class="form-control" dir="rtl" />
                </div>
            </div>

            <!-- Contact Information -->
            <div class="section-header">@(currentLang == "ar" ? "معلومات الاتصال" : "Contact Information")</div>
            <div class="form-row">
                <div class="form-group">
                    <label asp-for="MobilePhone" class="form-label"></label>
                    <input asp-for="MobilePhone" class="form-control" />
                </div>
                <div class="form-group">
                    <label asp-for="Email" class="form-label"></label>
                    <input asp-for="Email" class="form-control" type="email" />
                </div>
            </div>

            <div class="bilingual-row">
                <div class="bilingual-col english">
                    <label asp-for="Address" class="form-label"></label>
                    <textarea asp-for="Address" class="form-control" rows="2"></textarea>
                </div>
                <div class="bilingual-col arabic">
                    <label asp-for="AddressAr" class="form-label"></label>
                    <textarea asp-for="AddressAr" class="form-control" rows="2" dir="rtl"></textarea>
                </div>
            </div>

            <!-- Identification -->
            <div class="section-header">@(currentLang == "ar" ? "معلومات الهوية" : "Identification")</div>
            <div class="form-row">
                <div class="form-group">
                    <label asp-for="CPR_CR_Number" class="form-label"></label>
                    <input asp-for="CPR_CR_Number" class="form-control" />
                </div>
                <div class="form-group">
                    <label asp-for="VATNumber" class="form-label"></label>
                    <input asp-for="VATNumber" class="form-control" />
                </div>
                <div class="form-group">
                    <label asp-for="DateOfBirth" class="form-label"></label>
                    <input asp-for="DateOfBirth" class="form-control" type="date" />
                </div>
            </div>

            <div class="form-row">
                <div class="form-group">
                    <label asp-for="CPRExpiryDate" class="form-label"></label>
                    <input asp-for="CPRExpiryDate" class="form-control" type="date" />
                </div>
                <div class="form-group">
                    <label asp-for="PassportNo" class="form-label"></label>
                    <input asp-for="PassportNo" class="form-control" />
                </div>
                <div class="form-group">
                    <label asp-for="PassportIssueDate" class="form-label"></label>
                    <input asp-for="PassportIssueDate" class="form-control" type="date" />
                </div>
                <div class="form-group">
                    <label asp-for="PassportExpiryDate" class="form-label"></label>
                    <input asp-for="PassportExpiryDate" class="form-control" type="date" />
                </div>
            </div>

            <div class="form-group checkbox-group">
                <label class="checkbox-label"><input asp-for="IsActive" type="checkbox" /><span>Active</span></label>
            </div>

            <!-- Existing Documents -->
            <div class="section-header">
                @(currentLang == "ar" ? "المستندات الحالية" : "Existing Documents")
            </div>
            @if (Model.Documents?.Count > 0)
            {
                <div class="existing-documents">
                    @foreach (var doc in Model.Documents)
                    {
                        <div class="existing-doc-card" id="doc-@doc.Id">
                            <span class="doc-icon">📄</span>
                            <div class="doc-info">
                                <strong>@(currentLang == "ar" && !string.IsNullOrEmpty(doc.DocumentType?.NameAr) ? doc.DocumentType.NameAr : doc.DocumentType?.Name)</strong>
                                <small>@doc.FileName - @(doc.FileSize.HasValue ? $"{doc.FileSize.Value / 1024} KB" : "")</small>
                            </div>
                            <a href="/Customer/DownloadDocument?id=@doc.Id" class="btn-download-sm" title="@(currentLang == "ar" ? "تحميل" : "Download")">⬇️</a>
                            <button type="button" class="btn-delete-sm" onclick="deleteDocument(@doc.Id)" title="@(currentLang == "ar" ? "حذف" : "Delete")">🗑️</button>
                        </div>
                    }
                </div>
            }
            else
            {
                <p class="helper-text">@(currentLang == "ar" ? "لا توجد مستندات" : "No documents uploaded")</p>
            }

            <!-- Add New Documents Section -->
            <div class="section-header">
                @(currentLang == "ar" ? "إضافة مستندات جديدة" : "Add New Documents")
                <button type="button" class="btn-add-section" onclick="addDocument()">
                    <span>➕</span> @(currentLang == "ar" ? "إضافة مستند" : "Add Document")
                </button>
            </div>
            <div id="documentsContainer" class="dynamic-container">
                <p class="helper-text">@(currentLang == "ar" ? "انقر 'إضافة مستند' لرفع مستندات جديدة" : "Click 'Add Document' to upload new documents")</p>
            </div>

            <!-- Existing Vehicles -->
            <div class="section-header">
                @(currentLang == "ar" ? "المركبات الحالية" : "Existing Vehicles")
            </div>
            @if (Model.Vehicles?.Count > 0)
            {
                <div class="existing-vehicles">
                    @foreach (var vehicle in Model.Vehicles)
                    {
                        <div class="existing-vehicle-card" id="vehicle-@vehicle.Id">
                            <div class="vehicle-info">
                                <strong>@(currentLang == "ar" && !string.IsNullOrEmpty(vehicle.VehicleMake?.MakeNameAr) ? vehicle.VehicleMake.MakeNameAr : vehicle.VehicleMake?.MakeName)
                                        @(currentLang == "ar" && !string.IsNullOrEmpty(vehicle.VehicleModel?.ModelNameAr) ? vehicle.VehicleModel.ModelNameAr : vehicle.VehicleModel?.ModelName)</strong>
                                <small>@(currentLang == "ar" ? "رقم التسجيل:" : "Reg:") @vehicle.RegistrationNumber | @(currentLang == "ar" ? "الشاسيه:" : "Chassis:") @vehicle.ChassisNumber</small>
                            </div>
                            <button type="button" class="btn-delete-sm" onclick="deleteVehicle(@vehicle.Id)" title="@(currentLang == "ar" ? "حذف" : "Delete")">🗑️</button>
                        </div>
                    }
                </div>
            }
            else
            {
                <p class="helper-text">@(currentLang == "ar" ? "لا توجد مركبات" : "No vehicles registered")</p>
            }

            <!-- Add New Vehicles Section -->
            <div class="section-header">
                @(currentLang == "ar" ? "إضافة مركبات جديدة" : "Add New Vehicles")
                <button type="button" class="btn-import-excel" onclick="showImportModal()" style="position: absolute; right: 140px; top: 0;">
                    <span>📊</span> @(currentLang == "ar" ? "استيراد من Excel" : "Import from Excel")
                </button>
                <button type="button" class="btn-add-section" onclick="addVehicle()">
                    <span>➕</span> @(currentLang == "ar" ? "إضافة مركبة" : "Add Vehicle")
                </button>
            </div>
            <div id="vehiclesContainer" class="dynamic-container">
                <p class="helper-text">@(currentLang == "ar" ? "انقر 'إضافة مركبة' لتسجيل مركبة جديدة" : "Click 'Add Vehicle' to register new vehicle")</p>
            </div>

           <div class="form-actions">
                <button type="submit" class="btn-submit"><span>💾</span> @(currentLang == "ar" ? "حفظ التغييرات" : "Save Changes")</button>
                <a asp-action="Customers" class="btn-cancel">@(currentLang == "ar" ? "إلغاء" : "Cancel")</a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    <script>
        // Populate dropdown data from server
        window.documentTypesData = @Html.Raw(Json.Serialize(ViewBag.DocumentTypes ?? new List<object>()));
        window.vehicleMakesData = @Html.Raw(Json.Serialize(ViewBag.VehicleMakes ?? new List<object>()));
        window.vehicleYearsData = @Html.Raw(Json.Serialize(ViewBag.VehicleYears ?? new List<object>()));
        window.engineCapacitiesData = @Html.Raw(Json.Serialize(ViewBag.EngineCapacities ?? new List<object>()));

        // Delete document function
        function deleteDocument(docId) {
            const currentLang = localStorage.getItem('aegis-language') || 'en';
            if (confirm(currentLang === 'ar' ? 'هل أنت متأكد من حذف هذا المستند؟' : 'Are you sure you want to delete this document?')) {
                fetch('/Customer/DeleteDocument/' + docId, {
                    method: 'POST',
                    headers: {
                        'RequestVerificationToken': document.querySelector('input[name="__RequestVerificationToken"]').value
                    }
                })
                .then(response => response.json())
                .then(data => {
                    if (data.success) {
                        document.getElementById('doc-' + docId).remove();
                        alert(data.message);
                    } else {
                        alert(data.message);
                    }
                })
                .catch(error => console.error('Error:', error));
            }
        }

        // Delete vehicle function
        function deleteVehicle(vehicleId) {
            const currentLang = localStorage.getItem('aegis-language') || 'en';
            if (confirm(currentLang === 'ar' ? 'هل أنت متأكد من حذف هذه المركبة؟' : 'Are you sure you want to delete this vehicle?')) {
                fetch('/Customer/DeleteVehicle/' + vehicleId, {
                    method: 'POST',
                    headers: {
                        'RequestVerificationToken': document.querySelector('input[name="__RequestVerificationToken"]').value
                    }
                })
                .then(response => response.json())
                .then(data => {
                    if (data.success) {
                        document.getElementById('vehicle-' + vehicleId).remove();
                        alert(data.message);
                    } else {
                        alert(data.message);
                    }
                })
                .catch(error => console.error('Error:', error));
            }
        }
    </script>
    <script src="~/js/customer-form.js" asp-append-version="true"></script>
    <script src="~/js/vehicle-import.js" asp-append-version="true"></script>
}

<!-- Vehicle Import Modal -->
<div id="vehicleImportModal" class="modal" style="display:none;">
    <div class="modal-content">
        <div class="modal-header">
            <h2>@(currentLang == "ar" ? "استيراد المركبات من Excel" : "Import Vehicles from Excel")</h2>
            <span class="modal-close" onclick="closeImportModal()">&times;</span>
        </div>
        <div class="modal-body">
            <div class="import-instructions">
                <p>@(currentLang == "ar" ? "الخطوات:" : "Steps:")</p>
                <ol>
                    <li>@(currentLang == "ar" ? "قم بتحميل قالب Excel" : "Download the Excel template")</li>
                    <li>@(currentLang == "ar" ? "املأ بيانات المركبات" : "Fill in vehicle data")</li>
                    <li>@(currentLang == "ar" ? "قم برفع الملف" : "Upload the file")</li>
                </ol>
                <a href="/Customer/DownloadVehicleImportTemplate" class="btn-download-template">
                    <span>⬇️</span> @(currentLang == "ar" ? "تحميل القالب" : "Download Template")
                </a>
            </div>
            <div class="import-upload">
                <input type="file" id="vehicleImportFile" accept=".xlsx,.xls" style="display:none;" onchange="handleFileSelect(this)" />
                <button type="button" class="btn-select-file" onclick="document.getElementById('vehicleImportFile').click()">
                    <span>📁</span> @(currentLang == "ar" ? "اختر ملف Excel" : "Select Excel File")
                </button>
                <span id="selectedFileName" style="margin-left: 10px; color: #666;"></span>
            </div>
            <div id="importProgress" style="display:none;">
                <div class="progress-bar">
                    <div class="progress-fill"></div>
                </div>
                <p class="progress-text">@(currentLang == "ar" ? "جاري الاستيراد..." : "Importing...")</p>
            </div>
        </div>
        <div class="modal-footer">
            <button type="button" class="btn-import" onclick="startImport()" disabled id="btnStartImport">
                <span>📥</span> @(currentLang == "ar" ? "استيراد" : "Import")
            </button>
            <button type="button" class="btn-cancel-modal" onclick="closeImportModal()">
                @(currentLang == "ar" ? "إلغاء" : "Cancel")
            </button>
        </div>
    </div>
</div>

<!-- Import Results Modal -->
<div id="importResultsModal" class="modal" style="display:none;">
    <div class="modal-content">
        <div class="modal-header">
            <h2>@(currentLang == "ar" ? "نتائج الاستيراد" : "Import Results")</h2>
            <span class="modal-close" onclick="closeResultsModal()">&times;</span>
        </div>
        <div class="modal-body">
            <div class="results-summary">
                <div class="result-stat success">
                    <span class="stat-icon">✅</span>
                    <div>
                        <div class="stat-number" id="successCount">0</div>
                        <div class="stat-label">@(currentLang == "ar" ? "نجح" : "Successful")</div>
                    </div>
                </div>
                <div class="result-stat error">
                    <span class="stat-icon">❌</span>
                    <div>
                        <div class="stat-number" id="errorCount">0</div>
                        <div class="stat-label">@(currentLang == "ar" ? "فشل" : "Failed")</div>
                    </div>
                </div>
                <div class="result-stat total">
                    <span class="stat-icon">📊</span>
                    <div>
                        <div class="stat-number" id="totalCount">0</div>
                        <div class="stat-label">@(currentLang == "ar" ? "الإجمالي" : "Total")</div>
                    </div>
                </div>
            </div>
            <div id="errorsList" style="display:none;">
                <h3 style="color: #dc3545; margin-top: 20px;">@(currentLang == "ar" ? "الأخطاء:" : "Errors:")</h3>
                <div class="errors-table-container">
                    <table class="errors-table">
                        <thead>
                            <tr>
                                <th>@(currentLang == "ar" ? "الصف" : "Row")</th>
                                <th>@(currentLang == "ar" ? "الصانع" : "Make")</th>
                                <th>@(currentLang == "ar" ? "الموديل" : "Model")</th>
                                <th>@(currentLang == "ar" ? "الخطأ" : "Error")</th>
                            </tr>
                        </thead>
                        <tbody id="errorsTableBody">
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
        <div class="modal-footer">
            <button type="button" class="btn-primary" onclick="reloadPage()">
                <span>🔄</span> @(currentLang == "ar" ? "تحديث الصفحة" : "Refresh Page")
            </button>
            <button type="button" class="btn-cancel-modal" onclick="closeResultsModal()">
                @(currentLang == "ar" ? "إغلاق" : "Close")
            </button>
        </div>
    </div>
</div>

<style>
    .form-container { padding: 20px; max-width: 1000px; margin: 0 auto; }
    .form-header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 30px; }
    .form-header h1 { font-size: 28px; color: var(--navy-dark); display: flex; align-items: center; gap: 10px; }
    .btn-back { padding: 10px 20px; background: #6c757d; color: white; border-radius: 8px; text-decoration: none; transition: all 0.3s ease; }
    .btn-back:hover { background: #5a6268; }
    .form-card { background: white; border-radius: 12px; box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1); padding: 40px; }
    .section-header { font-size: 18px; font-weight: 700; color: var(--navy-dark); margin-bottom: 20px; margin-top: 30px; padding-bottom: 10px; border-bottom: 2px solid var(--primary-teal); }
    .section-header:first-of-type { margin-top: 0; }
    .form-row { display: grid; grid-template-columns: repeat(auto-fit, minmax(250px, 1fr)); gap: 20px; margin-bottom: 25px; }
    .form-group { margin-bottom: 25px; }
    .form-label { display: block; font-weight: 600; color: var(--navy-dark); margin-bottom: 8px; font-size: 14px; }
    .form-control { width: 100%; padding: 12px 15px; border: 2px solid #e0e0e0; border-radius: 8px; font-size: 15px; transition: all 0.3s ease; }
    .form-control:focus { outline: none; border-color: var(--primary-teal); box-shadow: 0 0 0 3px rgba(0, 166, 166, 0.1); }
    .checkbox-group { background: #f8f9fa; padding: 15px; border-radius: 8px; margin-top: 30px; }
    .checkbox-label { display: flex; align-items: center; cursor: pointer; font-weight: 500; }
    .checkbox-label input[type="checkbox"] { width: 20px; height: 20px; margin-right: 10px; cursor: pointer; accent-color: var(--primary-teal); }
    .form-actions { display: flex; gap: 15px; margin-top: 30px; }
    .btn-submit { flex: 1; padding: 14px 24px; background: linear-gradient(135deg, #ffc107, #ff9800); color: white; border: none; border-radius: 8px; font-size: 16px; font-weight: 600; cursor: pointer; display: flex; align-items: center; justify-content: center; gap: 8px; transition: all 0.3s ease; }
    .btn-submit:hover { transform: translateY(-2px); box-shadow: 0 6px 15px rgba(255, 193, 7, 0.3); }
    .btn-cancel { padding: 14px 24px; background: #6c757d; color: white; border-radius: 8px; text-decoration: none; font-weight: 600; transition: all 0.3s ease; }
    .btn-cancel:hover { background: #5a6268; }
    .text-danger { color: #dc3545; font-size: 13px; margin-top: 5px; display: block; }
    .validation-summary { background: #f8d7da; border: 1px solid #f5c6cb; color: #721c24; padding: 15px; border-radius: 8px; margin-bottom: 20px; }
    .bilingual-row { display: grid; grid-template-columns: 1fr 1fr; gap: 20px; margin-bottom: 25px; }
    .bilingual-col { display: flex; flex-direction: column; }
    .bilingual-col.arabic input, .bilingual-col.arabic textarea { font-family: 'Tajawal', 'Cairo', sans-serif; }
    .section-header { position: relative; }
    .btn-add-section { position: absolute; right: 0; top: 0; padding: 8px 16px; background: var(--primary-teal); color: white; border: none; border-radius: 6px; font-size: 13px; font-weight: 600; cursor: pointer; display: flex; align-items: center; gap: 5px; transition: all 0.3s ease; }
    .btn-add-section:hover { background: var(--teal-dark); transform: translateY(-2px); }
    .dynamic-container { min-height: 60px; margin-bottom: 30px; }
    .helper-text { color: #999; font-style: italic; text-align: center; padding: 20px; background: #f8f9fa; border-radius: 8px; border: 2px dashed #ddd; }
    .document-row, .vehicle-row { background: #f8f9fa; border-radius: 8px; padding: 20px; margin-bottom: 15px; border: 1px solid #ddd; }
    .existing-documents, .existing-vehicles { display: grid; grid-template-columns: repeat(auto-fill, minmax(300px, 1fr)); gap: 15px; margin-bottom: 20px; }
    .existing-doc-card, .existing-vehicle-card { background: #fff; border: 1px solid #ddd; border-radius: 8px; padding: 15px; display: flex; align-items: center; gap: 10px; }
    .doc-icon { font-size: 24px; }
    .doc-info { flex: 1; }
    .doc-info strong { display: block; margin-bottom: 3px; }
    .doc-info small { display: block; color: #666; font-size: 12px; }
    .btn-download-sm, .btn-delete-sm { padding: 6px 10px; color: white; border-radius: 5px; font-size: 16px; border: none; cursor: pointer; }
    .btn-download-sm { background: var(--primary-teal); text-decoration: none; }
    .btn-download-sm:hover { background: var(--teal-dark); }
    .btn-delete-sm { background: #dc3545; }
    .btn-delete-sm:hover { background: #c82333; }
    .existing-vehicle-card { flex-direction: row; align-items: center; justify-content: space-between; }
    .vehicle-info { flex: 1; }
    .existing-vehicle-card strong { display: block; margin-bottom: 5px; }
    .existing-vehicle-card small { color: #666; font-size: 13px; }
    .vehicle-row-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(200px, 1fr)); gap: 15px; }
    .btn-remove-doc, .btn-remove-vehicle { padding: 8px 16px; background: #dc3545; color: white; border: none; border-radius: 6px; font-size: 13px; cursor: pointer; display: flex; align-items: center; gap: 5px; }
    .btn-remove-doc:hover, .btn-remove-vehicle:hover { background: #c82333; }
    
    /* Import Excel Button */
    .btn-import-excel { padding: 8px 16px; background: #28a745; color: white; border: none; border-radius: 6px; font-size: 13px; font-weight: 600; cursor: pointer; display: flex; align-items: center; gap: 5px; transition: all 0.3s ease; }
    .btn-import-excel:hover { background: #218838; transform: translateY(-2px); }
    
    /* Modal Styles */
    .modal { position: fixed; top: 0; left: 0; width: 100%; height: 100%; background: rgba(0, 0, 0, 0.5); z-index: 9999; display: flex; align-items: center; justify-content: center; }
    .modal-content { background: white; border-radius: 12px; max-width: 600px; width: 90%; max-height: 90vh; overflow-y: auto; box-shadow: 0 10px 40px rgba(0,0,0,0.3); }
    .modal-header { padding: 20px 30px; border-bottom: 2px solid #e0e0e0; display: flex; justify-content: space-between; align-items: center; }
    .modal-header h2 { margin: 0; font-size: 22px; color: var(--navy-dark); }
    .modal-close { font-size: 28px; color: #999; cursor: pointer; line-height: 1; transition: color 0.3s; }
    .modal-close:hover { color: #333; }
    .modal-body { padding: 30px; }
    .modal-footer { padding: 20px 30px; border-top: 2px solid #e0e0e0; display: flex; gap: 10px; justify-content: flex-end; }
    
    /* Import Instructions */
    .import-instructions { background: #f8f9fa; padding: 20px; border-radius: 8px; margin-bottom: 20px; }
    .import-instructions ol { margin: 10px 0; padding-left: 20px; }
    .import-instructions li { margin: 5px 0; }
    .btn-download-template { display: inline-block; margin-top: 15px; padding: 10px 20px; background: var(--primary-teal); color: white; border-radius: 6px; text-decoration: none; font-weight: 600; transition: all 0.3s; }
    .btn-download-template:hover { background: var(--teal-dark); transform: translateY(-2px); }
    
    /* Import Upload */
    .import-upload { text-align: center; padding: 20px; border: 2px dashed #ddd; border-radius: 8px; margin: 20px 0; }
    .btn-select-file { padding: 12px 24px; background: #007bff; color: white; border: none; border-radius: 6px; font-weight: 600; cursor: pointer; transition: all 0.3s; }
    .btn-select-file:hover { background: #0056b3; }
    
    /* Progress Bar */
    .progress-bar { width: 100%; height: 20px; background: #e0e0e0; border-radius: 10px; overflow: hidden; margin: 20px 0; }
    .progress-fill { height: 100%; background: linear-gradient(90deg, var(--primary-teal), var(--teal-dark)); width: 0%; transition: width 0.3s; animation: pulse 1.5s infinite; }
    @@keyframes pulse { 0%, 100% { opacity: 1; } 50% { opacity: 0.7; } }
    .progress-text { text-align: center; color: #666; font-style: italic; }
    
    /* Modal Buttons */
    .btn-import { padding: 12px 24px; background: #28a745; color: white; border: none; border-radius: 6px; font-weight: 600; cursor: pointer; display: flex; align-items: center; gap: 8px; transition: all 0.3s; }
    .btn-import:hover:not(:disabled) { background: #218838; }
    .btn-import:disabled { background: #6c757d; cursor: not-allowed; opacity: 0.6; }
    .btn-cancel-modal { padding: 12px 24px; background: #6c757d; color: white; border: none; border-radius: 6px; font-weight: 600; cursor: pointer; transition: all 0.3s; }
    .btn-cancel-modal:hover { background: #5a6268; }
    .btn-primary { padding: 12px 24px; background: var(--primary-teal); color: white; border: none; border-radius: 6px; font-weight: 600; cursor: pointer; display: flex; align-items: center; gap: 8px; transition: all 0.3s; }
    .btn-primary:hover { background: var(--teal-dark); }
    
    /* Results Summary */
    .results-summary { display: grid; grid-template-columns: repeat(3, 1fr); gap: 20px; margin-bottom: 30px; }
    .result-stat { background: #f8f9fa; padding: 20px; border-radius: 8px; text-align: center; display: flex; flex-direction: column; align-items: center; gap: 10px; }
    .result-stat.success { border-left: 4px solid #28a745; }
    .result-stat.error { border-left: 4px solid #dc3545; }
    .result-stat.total { border-left: 4px solid #007bff; }
    .stat-icon { font-size: 36px; }
    .stat-number { font-size: 32px; font-weight: 700; color: var(--navy-dark); }
    .stat-label { font-size: 14px; color: #666; text-transform: uppercase; }
    
    /* Errors Table */
    .errors-table-container { max-height: 300px; overflow-y: auto; border: 1px solid #ddd; border-radius: 8px; }
    .errors-table { width: 100%; border-collapse: collapse; }
    .errors-table th { background: #f8f9fa; padding: 12px; text-align: left; font-weight: 600; position: sticky; top: 0; border-bottom: 2px solid #ddd; }
    .errors-table td { padding: 10px 12px; border-bottom: 1px solid #e0e0e0; }
    .errors-table tbody tr:hover { background: #f8f9fa; }
    
    @@media (max-width: 768px) {
        .bilingual-row, .form-row { grid-template-columns: 1fr; gap: 15px; }
        .results-summary { grid-template-columns: 1fr; }
        .modal-content { width: 95%; }
    }
</style>